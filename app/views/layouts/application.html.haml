!!! 5
%html{ :lang => "en"}
  %head
    %meta{ :charset => "utf-8" }

    %meta{ :name => "viewport", :content => "width=device-width, initial-scale=1.0" }

    %title= content_for?(:title) ? yield(:title) : "GM Intrusions!"

    = stylesheet_link_tag "application"
    = javascript_include_tag "vendor/modernizr"
    = csrf_meta_tag

  %body
    #page-wrap
      %header#header
        %nav#top-nav
          %ul.top-nav#top-nav-title
            %li.top-nav
              %h1= link_to 'GMIntrusions.us', root_path,
                class: 'top-nav-link'
          %ul.top-nav#top-nav-left
            %li.top-nav= link_to 'About', '#', class: 'top-nav-link'
            %li.top-nav= link_to 'Contact Us', '#', class: 'top-nav-link'
          %ul.top-nav#top-nav-right
            %li.top-nav= link_to 'New Intrusion', new_intrusion_path,
              class: 'top-nav-link'
            %li.top-nav= link_to 'New Tag', new_tag_path,
              class: 'top-nav-link'

        - if @error_msg
          - if @error_msg.respond_to? 'each'
            - @error_msg.each do |msg|
              .alert-box= msg
          - else
            .alert-box= @error_msg

        - if @warning_msg
          - if @warning_msg.respond_to? 'each'
            - @warning_msg.each do |msg|
              .warning-box= msg
          - else
            .warning-box= @warning_msg

        - if @success_msg
          - if @success_msg.respond_to? 'each'
            - @success_msg.each do |msg|
              .success-box= msg
          - else
            .success-box= @success_msg

      = yield

    %footer#footer
      %nav#bottom-nav
        %ul.bottom-nav
          %li.bottom-nav
            = link_to 'About', '#'
          %li.bottom-nav
            = link_to 'Contact Us', '#'
      #disclaimer-div
        = link_to 'Numenera and its logo are trademarks of Monte Cook Games, LLC in the U.S.A. and other countries. All Monte Cook Games characters and character names, and the distinctive likenesses thereof, are trademarks of Monte Cook Games, LLC. Content derived from Monte Cook Games publications is © 2013-2014 Monte Cook Games, LLC.',
        '#', id: 'disclaimer'

    = javascript_include_tag "application"
